#SUBDIRS = parser_example walk_hls
SUBDIRS =
SUBDIRS += file_example file_example/ifs tcplisten udplisten stdin_example

if !BUILD_OS_IS_DARWIN
if !MINGW
if !CYGWIN
#SUBDIRS += server_example
endif !CYGWIN
endif !MINGW
endif !BUILD_OS_IS_DARWIN
